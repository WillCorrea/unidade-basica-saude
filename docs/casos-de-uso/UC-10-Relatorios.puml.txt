@startuml
left to right direction
skinparam packageStyle rectangle

actor "Responsável Técnico (RT)" as RT
actor "Administrador Municipal" as Admin

rectangle "Sistema de Medicamentos UBS" {
  usecase "Selecionar Tipo de Relatório" as UC_SelectReport
  usecase "Definir Filtros (UBS/Período/etc.)" as UC_Filters
  usecase "Gerar Relatório" as UC_Generate
  usecase "Exportar PDF/CSV" as UC_Export
}

RT --> UC_SelectReport
RT --> UC_Generate
RT --> UC_Export

Admin --> UC_SelectReport
Admin --> UC_Generate
Admin --> UC_Export

UC_Filters ..> UC_SelectReport : <<include>>
UC_Generate ..> UC_Filters : <<include>>
UC_Export ..> UC_Generate : <<include>>
@enduml
